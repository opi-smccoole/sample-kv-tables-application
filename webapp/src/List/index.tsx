import React from 'react';
import { Api } from 'eosjs';

interface ListProps {
    api: Api | undefined
}

const List: React.FC<ListProps> = ({ api }: ListProps) => {
    return null;
}

export default List;