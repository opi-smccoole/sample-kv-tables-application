import React from 'react';

interface EntryProps {
    name: string,
    id: string,
    task: string,
    checked: boolean
}

const Entry: React.FC<EntryProps> = (entry: EntryProps) => {
    return null;
}

export default Entry;